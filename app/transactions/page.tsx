export const dynamic = "force-dynamic";
import { revalidatePath } from "next/cache";
import { prisma } from "../lib/prisma";
import { auDate, inputDate, money } from "../lib/finance";
import { buttonClass, inputClass, Panel, Shell } from "@/components/ui";
async function addTransaction(fd: FormData){"use server"; const amount=Number(fd.get("amount")||0), categoryId=String(fd.get("categoryId")||""), date=String(fd.get("date")||""); if(!amount||!categoryId||!date)return; await prisma.transaction.create({data:{type:String(fd.get("type"))==="INCOME"?"INCOME":"EXPENSE",amount,categoryId,merchant:String(fd.get("merchant")||"")||null,description:String(fd.get("description")||"")||null,date:new Date(date)}}); revalidatePath("/transactions"); revalidatePath("/");}
async function deleteTransaction(fd: FormData){"use server"; const id=String(fd.get("id")||""); if(id) await prisma.transaction.delete({where:{id}}); revalidatePath("/transactions"); revalidatePath("/");}
export default async function TransactionsPage(){const now=new Date(); const [categories,transactions]=await Promise.all([prisma.category.findMany({orderBy:{name:"asc"}}),prisma.transaction.findMany({include:{category:true},orderBy:{date:"desc"},take:100})]); return <Shell title="Transactions" subtitle="Search, filter, add and delete income or expense transactions."><Panel title="Add Transaction"><form action={addTransaction} className="grid grid-cols-1 md:grid-cols-3 gap-3"><select name="type" className={inputClass}><option value="EXPENSE">Expense</option><option value="INCOME">Income</option></select><input name="amount" type="number" step="0.01" placeholder="Amount" className={inputClass}/><select name="categoryId" className={inputClass}><option value="">Category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input name="merchant" placeholder="Merchant" className={inputClass}/><input name="description" placeholder="Description" className={inputClass}/><input name="date" type="date" defaultValue={inputDate(now)} className={inputClass}/><button className={buttonClass}>Add</button></form></Panel><Panel title="Transaction History"><div className="space-y-3">{transactions.map(t=><div key={t.id} className="flex justify-between items-center rounded-2xl bg-black/5 p-4"><div><p className="font-black">{t.merchant||t.description||t.category.name}</p><p className="text-sm text-black/50">{auDate(t.date)} • {t.category.name}</p></div><div className="flex gap-4 items-center"><p className={`text-xl font-black ${t.type==="INCOME"?"text-emerald-600":"text-red-600"}`}>{t.type==="INCOME"?"+":"-"}{money(t.amount)}</p><form action={deleteTransaction}><input type="hidden" name="id" value={t.id}/><button className="rounded-xl bg-red-100 text-red-700 px-4 py-2 font-bold">Delete</button></form></div></div>)}</div></Panel></Shell>}
